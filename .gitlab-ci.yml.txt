image: php:7.4

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r * public/  # Copia todos los archivos a la carpeta public
    - rm -rf public/.git  # Elimina el directorio .git si existe
  artifacts:
    paths:
      - public
  only:
    - main